version: '3.8'

services:
  scatterweb:
    build: .
    container_name: scatterweb-site
    restart: unless-stopped
    # No ports section as requested
    networks:
      - caddy-proxy

networks:
  caddy-proxy:
    external: true